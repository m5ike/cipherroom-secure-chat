node_modules/
dist/
.vite/

# SQLite database files — never commit; exposes demo data at rest
data.db
data.db-shm
data.db-wal
data.db-journal

.env
.env.*
!.env.example

*.log
.DS_Store

# Tilde backups from editors / shell append-to-filename files
*~
# Common editor swap files
.*.swp
*.bak

# Written by install.sh inside an install directory
.m5cet/

# Private keys / certificates (e.g. Vonage application key) — never commit
*.key
*.pem
